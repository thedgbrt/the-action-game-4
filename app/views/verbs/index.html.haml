%h1 Listing verbs

%table
  %thead
    %tr
      %th id
      %th Name
      %th Intensity
      %th Creator
      %th Examples
      %th Level
      %th
      %th

  %tbody
    - @verbs.each do |verb|
      %tr
        %td= verb.id
        %td= link_to verb.name, verb
        %td= link_to verb.description, edit_verb_path(verb)
        %td= verb.intensity
        %td= verb.creator.try(:name)
        %td= verb.examples
        %td= verb.level
        %td= link_to 'Destroy', verb, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Verb', new_verb_path
