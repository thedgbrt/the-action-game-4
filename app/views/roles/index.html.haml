%h1 Roles

%table#roles-table.compact
  %thead
    %tr
      %th Name
      %th Team
      %th Description
      %th
      %th
      %th

  %tbody
    - @roles.each do |role|
      %tr
        %td= role.name
        %td= role.team.try(:name)
        %td= role.description
        %td= link_to 'Show', role
        %td= link_to 'Edit', edit_role_path(role)
        %td= link_to 'Destroy', role, :method => :delete, :data => { :confirm => 'Are you sure?' }
