.clear-both
%table{style: "border: 0px; padding: 0px; margin: 0px"}
  %tbody
    %tr
      %td.mini-menu-action Currently Playing:
      - Aktion.current.sort_by{ |a| a.player.try(:name) }.each do |a|
        %td.mini-menu-action= link_to a.player.try(:name), a.player, title: a.summary_with_time_and_focus