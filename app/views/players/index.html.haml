%h1 Listing players

%table
  %thead
    %tr
      %th Name
      %th Provider
      %th Uid
      %th Role
      %th Email
      %th Fictional
      %th Current time zone
      %th
      %th
      %th

  %tbody
    - @players.each do |player|
      %tr
        %td= player.name
        %td= player.provider
        %td= player.uid
        %td= player.role
        %td= player.email
        %td= player.fictional
        %td= player.current_time_zone
        %td= link_to 'Show', player
        %td= link_to 'Edit', edit_player_path(player)
        %td= link_to 'Destroy', player, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Player', new_player_path
